{
    "webhook_url": "https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK",
    "webhook_token": "optional-bearer-token",
    "format": "batch",
    "_comment": "format can be 'batch' (all records in one request) or 'individual' (one request per record)"
}